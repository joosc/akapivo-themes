<?php

/**
 * @file
 * template.php
 */
 
 /*
function minorfront_links__locale_block(&$vars) {
  foreach($vars['links'] as $language => $langInfo) {
    $vars['links'][$language]['title'] = $language;
  }
  $content = theme_links($vars);
  return $content;
}
*/